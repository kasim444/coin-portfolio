import { ActionButtonGroups } from "@/components";
import { Container } from "@chakra-ui/react";

export const PortfolioTracker = () => {
  return (
    <Container maxWidth="container.xl" py="4">
      <ActionButtonGroups />
    </Container>
  );
};
